Return a single schema-valid JSON object. Use the exact field names and enum values in the
resolved output schema. Do not return a code fence, prose before or after JSON, hidden reasoning,
tool calls, URLs to fetch, or extra fields. When a value is unknown, use the schema's explicit
unknown/TBD/open-issue representation or leave the allowed field absent; do not fabricate it.
